<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Calculator" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/input_dark</item>
        <item name="colorPrimaryVariant">@color/input_dark</item>
        <item name="colorOnPrimary">@color/input_light</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/eq</item>
        <item name="colorSecondaryVariant">@color/back_dark</item>
        <item name="colorOnSecondary">@color/input_light</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:textColor">?attr/colorOnPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Calculator.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>